{
  "port": 2368, // Application port
  "database": { // MySQL DB
    "client": "mysql",
    "connection": {
      "host": "localhost",
      "user": "app",
      "password": "secret",
      "database": "app_dev",
      "charset": "utf8"
    }
  },

  "auth": {
    "secret": "secret" // JWT secret
  },

  "session": {
    "secret": "another secret", // Web cookie secret
    "store": { // Redis db details
      "host": "localhost",
      "port": 6379
    }
  }
}
