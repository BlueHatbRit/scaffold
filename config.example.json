{
  "port": 2368,
  "database": {
    "client": "mysql",
    "connection": {
      "host": "localhost",
      "user": "scaffolduser",
      "password": "mydbpassword",
      "database": "scaffold",
      "charset": "utf8" // The database must be utf8 charset for MySQL
    }
  },

  "auth": {
    "secret": "secret" // For Json Web Tokens
  },
  
  "session": {
    "secret": "web session secret", // For session cookies
    "store": {
      "host": "localhost", // Redis host
      "port": 6379 // Redis port
    }
  }
}
